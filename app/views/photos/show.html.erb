<div class="card photo-form">
<div class="card-body">
<p>
  <strong>Description:</strong>
  <%= @photo.description %>
	<br>
  <strong>User Email:</strong>
	<%= @photo.user.email if @photo.user %>
</p>

<% if @photo.user == current_user %> <%= link_to 'Edit', edit_photo_path(@photo) %> |<% end %>
<%= link_to 'Back', photos_path %>
	</div>
</div>